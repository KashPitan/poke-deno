{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read server.ts",
      "desc": "run my app.ts file"
    },
    "docker-pokeapp": {
      "cmd": "docker run -p 3000:3000 pokeapp",
      "desc": "run docker container"
    }
  }
}
